global:
  scrape_interval: 30s
  evaluation_interval: 60s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["${collector_host}:9464"]
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
